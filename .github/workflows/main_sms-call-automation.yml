# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App - sms-call-automation

on:
  push:
    branches:
      - main
  workflow_dispatch:
env:
    CONNECTION_STRING: 'endpoint=https://sms-acs-demo.canada.communication.azure.com/;accesskey=C206JCSg1iUEQzKBqrnapWhqE9LO2wGOab1Ru8CC5me5IgaP2RKqJQQJ99BFACULyCpvTPyMAAAAAZCS1iCI'
    ACS_RESOURCE_PHONE_NUMBER: '+14388140297'
    COGNITIVE_SERVICES_ENDPOINT: 'https://sms-demo-multi-ai-service-classic.cognitiveservices.azure.com/'
    REDIS_CONNECTION_STRING: 'redis://default:b7tA6vMnf4GPckK85BvIyCPFeqze8zQJ@redis-14401.c92.us-east-1-3.ec2.redns.redis-cloud.com:14401'
    PUBLIC_URL: 'sms-call-automation-bjgua5g7hjg3craw.canadacentral-01.azurewebsites.net'
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '20.x'

      - name: npm install, build, and test
        run: |
          npm install
          npm run build --if-present
          npm run test --if-present

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: node-app
          path: .

  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: node-app
      
      - name: 'Deploy to Azure Web App'
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'sms-call-automation'
          slot-name: 'Production'
          package: .

          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_2B86BA473B3841CEA5E3132C4FE11B6C }}
